# Fable's response to Astra round 1 (CAS sync, 26 Sep 2026)

Every claim checked against the source before acting.

| ID | Verdict | Checked where | Action |
|---|---|---|---|
| CAS-01 | AGREE | `TaskContext.tsx` flush: `void storage.cloudSaveByToken(token, merged)` drops the result, while `storage.synced` already holds the merged card and its new rev. The next save writes the unmerged React state with that rev and succeeds. Real loss path. | Every caller adopts a merged card. TaskContext adopts it into React state with a three-way merge (base = what it submitted, local = the current state, remote = the merged card), in the timer and in the flush. No reload. |
| CAS-02 | AGREE (severity) | Timer: `storage.save(written.state)` then reload drops any tap made during the round trip. | Same fix as CAS-01: `setState(prev => mergeStates(submitted, prev, merged))` keeps the taps made meanwhile; the next save writes them with the new rev. No freeze, no reload. |
| CAS-03 | AGREE | `mergeState.ts:262-263`: the "only one side changed" shortcut runs before `expeditionRule`, so an unchanged-looking local `home` (a trip done and back while offline) loses to the card's `away` for the same trip. | `expedition` is resolved by its rule always (adventureCount first, then how far along), never by the shortcut. Test added for Astra's case. |
| CAS-04 | AGREE | `overlapDone` requires base, local and remote on the same day. | Overlap counted whenever local and remote are on the same day; tasks done in a same-day base are excluded, otherwise every task done on both counts once. |
| (new) | own find | `totalQuestCompletions` is a map `{taskId: count}` (`TaskContext.tsx` complete()), but it was in the number-delta set: `num(object)` is 0, so a race would flatten the map to a number. | Per-task delta map rule, with the overlap taken out per task. Test added. |
| CAS-05 | PARTLY | True, and not new: before this branch every write was last-writer-wins, and the cold-start choice is unchanged. But the fix is small now that the merge exists. | Persist `{rev, state}` per card after every acknowledged read or write (removed if it cannot be stored, so a stale base never survives). Cold start: same rev as the stored base, local is the newer copy and is written with CAS; different rev, three-way merge of stored base, local and card. Without a stored base the old date rule stays. |
| CAS-06 | AGREE | `missingFunction` matched any message naming the function (a permission error would downgrade to the unconditional write), and the downgrade lasted the page's lifetime. | Only PGRST202 or 42883 count as missing; the downgrade expires after 10 minutes and CAS is tried again. Rollout: migration first, check it live, then the client. Old bundles stay last-writer-wins until they update; accepted as a short transition. |
| CAS-07 | AGREE | Line 268: with a base, a field without a rule goes to this device, as the file header says. My own read said "remote", which was wrong. | Own read corrected here. Local-wins is kept for settings-like fields; `dailyHabits` gets a per-key rule so ticks from both devices survive. |

Known limits kept (written into HANDOFF): rewards that fire on both devices from the same event outside task ticks (a legacy weekly mission finishing at a day change that both devices run from the same card, the cat care flags, water sips) can still be counted twice in a race. The worst case is extra Sterne, never lost progress. Two tabs of the app open on one device share one stored base.
