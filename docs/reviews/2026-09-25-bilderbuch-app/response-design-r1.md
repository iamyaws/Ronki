# Response to Astra's design review, round 1 (25 Sep 2026)

Astra's verbatim output: `astra-design-r1.txt` (task, medium effort, verdict NOT YET). Own read written first: `own-read-design-r1.md`. Independent agreement: home density (own read 5, Astra R3) and animation weight (own read 6, Astra R8). Astra found five things my read missed (R1, R2, R4, R5, R7); my read found two Astra did not raise (egg tile size, parent setup egg under the banner), both fixed before this response.

| ID | Astra | Response | What changed |
|---|---|---|---|
| R1 BLOCKER | After two days away TaskContext sets `besorgt`, and home and the pinned Ronki still show a worried face | **AGREE** | New `ambientMood()` in `MoodChibi.jsx` shows `besorgt` as calm; used by RoomHub, PinnedRonki, RonkiPortrait (when no mood prop is passed) and the profile. Feelings exercises that pass `besorgt` on purpose still get the worried face. State untouched. |
| R2 SHOULD | Task pictures do not explain the tasks (sparkle for teeth, star for dressing, egg for meals); the read-aloud button calls the muted narrator | **AGREE** | One generated sheet of 12 crayon task pictures in the Ronki style (Higgsfield GPT Image 2.5, 2.75 credits), cut into `public/art/bilderbuch/tasks/`: waking, toothbrush, glass of water, washcloth, plate, book, homework, pyjamas, shirt, school bag, ball, night light. Washing got its own family. `TaskPicture` in RonkisTag with the old doodle as fallback. Found on the way: "Zähne putzen" never matched the teeth rule (`zahn` vs `zähn`), fixed. The read-aloud button is gone until a Ronki voice line replaces the muted narrator. |
| R3 SHOULD | The current action sits below too much content on home and in the day strip | **PARTLY** | Day strip: opens scrolled to the block that is "now" (afternoon, evening); order and completion rules unchanged. Home: kept. The room with Ronki is the first thing on purpose, the day is one tap away in the tab bar, and fitting six feeling tiles above the fold would shrink the room to a strip. Listed for Marc as a layout decision (home order). |
| R4 SHOULD | `animated` replaces grown and legendary Ronki with the young idle loop | **AGREE** | `resolveRonkiArt` resolves stages 4 and 5 before the loop; the loop only plays for stages 2 and 3; grown and legendary breathe with the CSS idle. Tests added. |
| R5 SHOULD | The routine-complete card mounts the 4 s cheer clip; celebration screens keep 4 to 5 s countdown buttons | **PARTLY** | The card now shows the still cheer with a short CSS bob; the burst stays at 1.2 s. The countdown on the celebration buttons is kept: it is a pre-existing guard against a kid tapping through by accident, not a timer that asks the kid to hurry, and removing it changes behaviour. Listed for Marc. |
| R6 SHOULD | Sound is not off by default | **DISAGREE** | `utils/voiceAudio.ts` records Marc's April 2026 decision: new users hear Ronki by default because the voice bank is the reading aid for kids who cannot read yet. My lane brief's "sound stays off by default" came from the research memo's generic guardrail and was wrong for this app; corrected. Listed for Marc as a challenge, not changed. |
| R7 SHOULD | "Okay" and "Magisch" change picture and colour between home and diary | **AGREE** | Home's mood tiles now draw `FeelingDoodle` from `JournalFeelings.jsx`, the same map the diary and the book use. Stored values unchanged. |
| R8 SHOULD | Animated Ronki and the room video keep decoding behind overlays and off screen | **AGREE** | `MoodChibi` mounts the loop only while it is on screen (IntersectionObserver), the tab is visible, and someone tapped in the last 60 s; otherwise the still, which now has the same framing. `SceneLoop` gained `paused`; RoomHub pauses the room video and stops Ronki's loop while the expedition, the sitting overlay or the style sheet is open. |
| R9 COULD | A2 to A5 are unproven without a real device and a real first grader | **AGREE** | Not fixable from here. Marc's walk-through on his phone and Louis on the family tablet are the tests; the handoff lists them as the first step. |

Checks after the fixes: 222 tests green, `npm run check:names` clean, 23 pre-existing tsc errors unchanged, build green, browser pass on the day strip (opens on "Abend", all task pictures load, teeth fixed).

Open for round 2: R3 (home order), R5 (countdown), R6 (sound default).
